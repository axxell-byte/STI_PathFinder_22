<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.stipathfinder">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.STIPathFinder"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- Launcher Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Other App Activities -->
        <activity android:name=".MainActivity"/>
        <activity android:name=".PathfinderDeveloperActivity"/>
        <activity android:name=".PathfinderDescriptionActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".NavigationActivity"/>

        <!-- Godot library activity (no launcher) -->
        <activity
            android:name="com.godot.game.GodotApp"
            android:exported="true"
            android:screenOrientation="landscape"
            android:resizeableActivity="false"
            android:theme="@style/GodotAppSplashTheme"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|density|keyboard|navigation|screenLayout|uiMode"
            tools:node="merge"
            tools:ignore="UnusedAttribute">
            <meta-data
                android:name="org.godotengine.editor.version"
                android:value="${godotEditorVersion}" />
        </activity>

        <!-- FileProvider for Godot -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:node="merge">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/godot_provider_paths" />
        </provider>

        <!-- Microsoft Identity Browser Activity -->
        <activity android:name="com.microsoft.identity.client.BrowserTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="msalc1754644-7f36-4e40-9ac7-6441601d3866"
                    android:host="auth"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
